name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install and test
      run: |
        cd TATS
        npm i -g ionic
        npm install
        npm run build
        npm run test
